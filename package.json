{
  "name": "student-os-monorepo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently --kill-others-on-fail --prefix-colors \"cyan,magenta,yellow,blue\" --names \"shared,backend,frontend,admin\" \"pnpm --filter @student-os/shared run dev\" \"pnpm --filter @student-os/backend run dev\" \"pnpm --filter @student-os/frontend run dev\" \"pnpm --filter @student-os/admin run dev\"",
    "build": "pnpm --filter @student-os/shared run build && pnpm --filter @student-os/config run build && pnpm --filter @student-os/ui run build && pnpm --filter @student-os/backend run build && pnpm --filter @student-os/frontend run build && pnpm --filter @student-os/admin run build",
    "lint": "eslint .",
    "format": "prettier --write \"**/*.{ts,tsx,json,css,md}\"",
    "typecheck": "pnpm -r run typecheck"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.24.0",
    "@typescript-eslint/parser": "^8.24.0",
    "concurrently": "^9.1.2",
    "eslint": "^9.20.1",
    "eslint-plugin-react-hooks": "^5.1.0",
    "eslint-plugin-react-refresh": "^0.4.19",
    "prettier": "^3.5.1",
    "typescript": "^5.7.3"
  }
}
